name: Check Pull Request

on:
  schedule:
    - cron: '0 7 * * 1'
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

jobs:
  parse-depends-on:
    name: Resolve branch dependencies
    runs-on: ubuntu-latest
    outputs:
      backend-branch: ${{ steps.resolve.outputs.backend-branch }}
      harness-branch: ${{ steps.resolve.outputs.harness-branch }}
    steps:
      - name: Resolve branches from PR body
        id: resolve
        env:
          PR_BODY: ${{ github.event.pull_request.body || '' }}
        run: |
          backend_dep=$(echo "$PR_BODY" | grep -oP '(?<=Depends-On: DEFRA/bng-metric-backend#)\S+' | head -1)
          harness_dep=$(echo "$PR_BODY" | grep -oP '(?<=Depends-On: DEFRA/bng-metric-harness#)\S+' | head -1)

          echo "backend-branch=${backend_dep:-main}" >> "$GITHUB_OUTPUT"
          echo "harness-branch=${harness_dep:-main}" >> "$GITHUB_OUTPUT"

          echo "Resolved backend branch: ${backend_dep:-main}"
          echo "Resolved harness branch: ${harness_dep:-main}"

  integration-tests:
    name: Integration Tests
    needs: parse-depends-on    
    uses: DEFRA/bng-metric-harness/.github/workflows/integration-tests.yml@${{ needs.parse-depends-on.outputs.harness-branch }}
    with:
      frontend-branch: ${{ github.head_ref }}
      backend-branch: ${{ needs.parse-depends-on.outputs.backend-branch }}
      harness-branch: ${{ needs.parse-depends-on.outputs.harness-branch }}
    secrets: inherit

  pr-validator:
    name: Run Pull Request Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Test code and Create Test Coverage Reports
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: |
          npm ci
          npm run build:frontend
          npm run format:check
          npm run lint
          npm test

      - name: Security audit
        run: npm run security-audit

      - name: Test Docker Image Build
        run: |
          set +e
          docker build --no-cache --tag cdp-node-frontend-template .
          exit $?

#      - name: SonarCloud Scan
#        if: github.actor != 'dependabot[bot]'
#        uses: SonarSource/sonarqube-scan-action@v5.3.1 #  (or check latest version on https://github.com/marketplace/actions/official-sonarqube-scan)
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
